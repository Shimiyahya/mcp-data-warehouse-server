# Copy to `.env` and fill in. Required ONLY for the live demo (demo.py).
# The server, seeder, and test suite do NOT need an API key.
# Never commit your real key.
ANTHROPIC_API_KEY=

# Optional: point the server at a different DuckDB file (defaults to ./warehouse.duckdb)
# MCP_WAREHOUSE_DB=
