apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-data-warehouse.fullname" . }}
  labels:
    {{- include "mcp-data-warehouse.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "mcp-data-warehouse.selectorLabels" . | nindent 4 }}
